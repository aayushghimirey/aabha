<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only. Cleartext is allowed to the loopback addresses a local
  uvicorn listens on, and nowhere else.

  10.0.2.2 is how the Android emulator reaches the host machine's 127.0.0.1.
  Running against a physical device over the LAN means adding that machine's
  address here as another <domain>, e.g. 192.168.1.20.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
